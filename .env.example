# Anthropic API key for the PDF contract-extraction agent (server-side only).
# Copy this file to .env.local and paste your key. Never commit .env.local.
# Get a key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
